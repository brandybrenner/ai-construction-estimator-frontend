# Frontend for AI Construction Estimator
- Next.js 14 + Tailwind + RHF/Zod
## Dev
1) `cp .env.example .env.local` and set `NEXT_PUBLIC_API_URL` to your backend.
2) `npm install` and `npm run dev` (http://localhost:3000)